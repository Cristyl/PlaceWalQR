<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="280dp"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="12dp"
    android:background="@android:color/white"
    android:elevation="4dp">

    <!-- Immagine del posto -->
    <ImageView
        android:id="@+id/photo_marker"
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:scaleType="centerCrop"
        android:background="@android:color/darker_gray"
        android:layout_marginBottom="8dp"
        android:contentDescription="Foto del luogo" />

    <!-- Titolo del posto -->
    <TextView
        android:id="@+id/title_marker"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Nome del posto"
        android:textStyle="bold"
        android:textSize="16sp"
        android:textColor="@android:color/black"
        android:layout_marginBottom="4dp"
        android:maxLines="1"
        android:ellipsize="end" />

    <!-- Descrizione del posto -->
    <TextView
        android:id="@+id/description_marker"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Descrizione del luogo"
        android:textSize="14sp"
        android:textColor="@android:color/darker_gray"
        android:layout_marginBottom="8dp"
        android:maxLines="3"
        android:ellipsize="end" />

    <!-- Riga con informazioni aggiuntive (opzionale) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <!-- Status visitato/non visitato -->
        <TextView
            android:id="@+id/status_marker"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="12sp"
            android:background="@android:color/transparent"
            android:padding="4dp" />

        <!-- Coordinate (opzionale) -->
        <TextView
            android:id="@+id/coordinates_marker"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textSize="11sp"
            android:textColor="@android:color/darker_gray"
            android:gravity="end"
            android:padding="4dp" />

    </LinearLayout>

</LinearLayout>